# Password for protected blog posts. The static build will only embed it when
# EMBED_WIP_PASSWORD=true, because this repo is public.
EMBED_WIP_PASSWORD=false
WIP_PASSWORD=your_password_here

# Optional local values for serving/testing Supabase Edge Functions.
SUPABASE_URL=
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=
AGENT_GUESTBOOK_SALT=
AGENT_GUESTBOOK_ALLOWED_ORIGINS=https://parthsareen.com,https://parthsareen.github.io,http://127.0.0.1:8080,http://localhost:8080
AGENT_GUESTBOOK_HOURLY_LIMIT=3
AGENT_GUESTBOOK_DAILY_LIMIT=10
AGENT_GUESTBOOK_MAX_ROWS=500

# Recommended readings are publicly readable. The /rec-reading skill uses a
# local secret key above to insert; never put that key in the static site.
RECOMMENDED_READINGS_ALLOWED_ORIGINS=https://parthsareen.com,https://parthsareen.github.io,http://127.0.0.1:8080,http://localhost:8080
RECOMMENDED_READINGS_PUBLIC_LIMIT=100
